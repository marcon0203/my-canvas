# 统一错误类型。所有包都依赖它 —— 放进任何一个功能包都会造出假依赖。
[package]
name = "studio-error"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
# 只有测试用：验「序列化成前端认识的 { code, message }」
serde_json.workspace = true
